{
  "buildCommand": "prisma generate && next build",
  "crons": [
    {
      "path": "/api/cron/reactivate-frozen-memberships",
      "schedule": "0 0 * * *"
    }
  ],
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 10
    }
  }
}
